import { Meta, StoryObj } from "@storybook/react";

import PopUp from ".";

export default {
  title: "components/PopUp",
  component: PopUp,
  parameters: {
    layout: "fullscreen",
  },
} as Meta;

export const Default: StoryObj = {};
